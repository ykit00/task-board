<% provide(:submit_button_text, '更新') %>

<div class="container">
  <header>
    <%= render 'navbar' %>
    <div class="flash-message mb-5">
      <% flash.each do |message_type, message| %>
        <%= content_tag(:div, message, class: "alert alert-#{message_type}") %>
      <% end %>
    </div>
  </header>
  <div class="row">
    <div class="mx-auto">
      <h2>タスク編集</h2>
    </div>
  </div>
  <%= render 'form', task: @task %>
</div>
