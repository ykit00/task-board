<% provide(:submit_button_text, '作成') %>

<div class="container">
  <header>
    <%= render 'navbar' %>
    <%= render 'shared/flash_message' %>
  </header>
  <div class="row">
    <div class="mx-auto">
      <h2>新規タスク</h2>
    </div>
  </div>
  <%= render 'form', task: @task %>
</div>
